To run the project

1. Install node.
2. Check if node is installed in cmd(node -v, npm -v)
3. Go to backend and frontend directories and install dependencies(npm install)
4. Start backend: Go to backend directory(TCG Ladder - root)(npm start)
5. Start frontend: Go to frontend directory(tcg-ladder-frontend)(npm start)